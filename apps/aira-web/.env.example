# API Configuration
# Use /api for local dev to avoid CORS (Next.js proxies to API_UPSTREAM).
# Use full URL (e.g. https://dev.api.airaai.in) when API allows your origin.
NEXT_PUBLIC_API_BASE_URL=
NEXT_PUBLIC_API_TIMEOUT=30000
# Upstream API for proxy (set when NEXT_PUBLIC_API_BASE_URL=/api)
API_UPSTREAM=

# Dev only: paste your access token here to skip login (never commit .env.local!)
# NEXT_PUBLIC_DEV_ACCESS_TOKEN=

# Google OAuth
NEXT_PUBLIC_GOOGLE_AUTH_URL=
